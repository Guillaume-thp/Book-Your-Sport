<DOCTYPE html>
    <html>

    <head>
        <title>Timeslot</title>
        <link href="https://fonts.googleapis.com/css?family=Montserrat&display=swap" rel="asset/stylesheet">
    </head>



    <tbody>

        <body>
            <p id="notice">
                <%= notice %>
            </p>
            <%= notice %>
                </p>
                <h1 class="Montserrat">Voici les créneaux disponibles pour la ville de
                    <%=params[:city][:choice]%> à
                        <%=params[:time][:choice]%> le
                            <%=params[:date]%>
                </h1>
                <table>
                    <thead>
                        <tr>
                            <th colspan="3"></th>
                        </tr>
                    </thead>
                    <tbody>
                        <% scrap = ScrapLeFive.new.perform(params[:city][:choice], params[:time][:choice], params[:duration][:choice],params[:date]) %>
                            <p id="notice">
                                <%= notice %>
                            </p>
                            <br><br>
                            <br>
                            <table>
                                <thead>
                                    <tr>
                                        <th colspan="3"></th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% i = 0 %>
                                        <% scrap[0].length.times do%>
                                            </div>
                                            </div>
                                </tbody>
                            </table>
                            <div class="center">
                                <div class=" timeslot-style card border-warning mb-3 center">
                                    <div class="card-body center">
                                        <h4 class="card-title center front-size ">

                                            <%= date = scrap[0][i]%>
                                                <%= hour = scrap[1][i]%>
                                                    <%= duration = scrap[2][i]%>
                                                        <%= amount = scrap[3][i].delete(' ') %>
                                                            <%= if current_user
                                                        link_to "Réserver", new_charge_path(date:date, hour:hour, duration:duration, amount:amount), class: "btn btn-warning"
                                                        else " "
                                                        end %>

                                        </h4>
                                    </div>
                                </div>
                            </div>
                            </div>

                            <% i = i+1 %>
                                <% end %>

        </body>

    </html>

    <html>

    </html>